apiVersion: v1
kind: Secret
metadata:
  name: {{ include "redis.fullname" . }}-secret
  labels:
  {{- include "redis.labels" . | nindent 4 }}
stringData:
  REDIS_PASSWORD: {{ required "secret.redisPassword is required" .Values.secret.redisPassword
    | quote }}